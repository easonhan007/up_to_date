<%= render @crawler_record %>

<div class="d-flex gap-4">
  <%= link_to "Edit", edit_crawler_record_path(@crawler_record), class: 'btn btn-primary' %> 
  <%= link_to "Back", crawler_records_path, class: 'btn btn-secondary' %>

  <%= button_to "Destroy", @crawler_record, method: :delete, class: 'btn btn-danger' %>
</div>
